import React from 'react';

const Service = ({service}) => {
  /* console.log(service?.name);
  const {name}=service */
  return (
    <div>
      <h1></h1>
    </div>
  );
};

export default Service;